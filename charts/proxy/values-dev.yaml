# Default values for s3-proxy.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""

oauth:
  signin: ""
  url: ""

baseHostname: proxy.dev8.bip.va.gov
endpoints:
  no_auth:
    - host: vefs-rrc-dev
      endpoint: https://project-vefs-rrc-dev-bucket.s3.us-gov-west-1.amazonaws.com
    - host: vefs-rrc-int
      endpoint: https://project-vefs-rrc-int-bucket.s3.us-gov-west-1.amazonaws.com
    - host: vefs-rrc-tst
      endpoint: https://project-vefs-rrc-tst-bucket.s3.us-gov-west-1.amazonaws.com

secretName: proxy-dev-secret

deployment:
  replicaCount: 2
  image:
    repository: nginx
    tag: '1.18'
    pullPolicy: IfNotPresent
  resources: {}
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  nodeSelector: {}
  tolerations: []
  affinity: {}

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
